from threading import Thread
from numpy import save

class SaveThread(Thread):
    def __

    def start(self):
        Thread().start()

    def save_file(self, file_name, training_data):
        save(file_name, training_data)